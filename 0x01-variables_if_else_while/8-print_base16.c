#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	char ch;
	int n;

	for (ch = 'a'; ch <= 'f'; n++)
{

	putchar(n);
}
	for (ch = 'a'; ch <= 'f'; ch++)
{

	putchar(ch);
}

	putchar(10);

	return(0);
}
